name: project1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  animated_splash_screen: ^1.3.0
  page_transition: "^2.1.0" #스플래쉬 화면이후 페이지 나타는방법
  lottie: ^3.1.2
  http: ^1.2.2
  shared_preferences: ^2.0.6
  kakao_flutter_sdk: ^1.9.5 # 전체 추가
  #kakao_flutter_sdk_user: ^1.9.5 # 카카오 로그인 API 패키지
  #kakao_flutter_sdk_share: ^1.9.5 # 카카오톡 공유 API 패키지
  #kakao_flutter_sdk_talk: ^1.9.5 # 카카오톡 채널, 카카오톡 소셜, 카카오톡 메시지 API 패키지
  #kakao_flutter_sdk_friend: ^1.9.5 # 피커 API 패키지
  #kakao_flutter_sdk_navi: ^1.9.5 # 카카오내비 API 패키지
  flutter_feather_icons: ^2.0.0 # 상단바 아이콘
  intl: ^0.18.0 #날짜
  stop_watch_timer: #타이머


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/demo1.png
    - assets/images/demo2.png
    - assets/study.json
